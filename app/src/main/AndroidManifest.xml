<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/splashball"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_balon"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChuteApp"
        tools:targetApi="31">
        <activity
            android:name=".StoreList"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Ingreso"></activity>
        <activity android:name=".Equipos"></activity>
        <activity android:name=".StoreView"/>
    </application>

</manifest>